default_install_hook_types: [pre-commit, commit-msg]
default_stages: [pre-commit]
fail_fast: false

# 各 hook の rev は `just upgrade-hooks` (pre-commit autoupdate) で latest 追従。
# Dependabot は .pre-commit-config 直接 update できないため運用で autoupdate。

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: mixed-line-ending
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
        args: [--maxkb=2048]
      - id: check-case-conflict
      - id: detect-private-key

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  # basedpyright is run via `just lint-static` (uv) — keeping it out of
  # pre-commit avoids version drift between the mirror tag and the project
  # pin in pyproject.toml.

  - repo: https://github.com/PyCQA/bandit
    rev: 1.9.3
    hooks:
      - id: bandit
        args: ["-c", "pyproject.toml", "-r", "src"]
        additional_dependencies: ["bandit[toml]"]

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks

  - repo: https://github.com/crate-ci/typos
    rev: v1.26.8
    hooks:
      - id: typos

  - repo: https://github.com/compilerla/conventional-pre-commit
    rev: v3.4.0
    hooks:
      - id: conventional-pre-commit
        stages: [commit-msg]
        args:
          - feat
          - fix
          - perf
          - refactor
          - style
          - test
          - build
          - ci
          - docs
          - chore
          - revert
